# Sample persona skill (placeholder)

This directory will host a scrubbed example of a generated `persona-{nickname}` skill so newcomers can see expected output shape without running the generator.

Not yet populated. See `docs/architecture.md` for the output contract.
